buildAndPackageLib("${PROJECT_NAME}"
	COMPONENT "libtermiwin"
	PUBLIC_INCLUDES ${MAIN_INC_DIR}
)
if (TERMIWIN_DONOTREDEFINE)
	target_add_definitions("${PROJECT_NAME}" PUBLIC "-DTERMIWIN_DONOTREDEFINE")
endif (TERMIWIN_DONOTREDEFINE)

